"""
This __init__.py file must be kept empty to prevent circular imports.

The application's dependency structure requires that modules are imported
directly where they are needed, rather than being exposed through this
package-level __init__.py.
"""
